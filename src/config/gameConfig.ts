/**
 * Game configuration constants.
 * Centralized for easy tuning and consistency across Phaser and game systems.
 */

export const TILE_SIZE = 32;
export const MAP_WIDTH = 50; // tiles
export const MAP_HEIGHT = 50; // tiles
export const FOV_RADIUS = 8;
export const PLAYER_MOVE_DURATION = 150; // ms for movement tween
export const PLAYER_MOVE_DURATION_SLOW = 300; // ms when moving onto flooded tile
